using C = System.Console;
using M = System.Math;

class S
{
    static void Main()
    {
        for (int l = int.Parse(C.ReadLine()); l-- > 0; )
        {
            int i = int.Parse(C.ReadLine());
            C.WriteLine(i);
        }
    }
}
